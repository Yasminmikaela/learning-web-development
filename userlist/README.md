# Exercise: Userlist

### Task:
Create a React app called "Userlist" that fetches user information from an external API and presents it in a structured table.

### What I learned:
- Fetching data from an API using `useEffect`.
- Displaying list data dynamically using the `.map()` method.
- Basic table structuring in React.
- Solving API connection issue CORS.